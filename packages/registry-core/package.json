{
  "name": "@rack/registry-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared registry protocol primitives — URL parsing, key building, segment derivation, listing — for the Rack registry server and worker.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
